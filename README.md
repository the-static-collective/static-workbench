# Static Workbench

A local-first browser workbench for a dedicated Static Collective Linux machine.

This repository is being initialized from the verified **v0.2 — HOUSE** source bundle. The browser is the desk; the supervisor is the local process boundary; project-native identities and receipts remain owned by their projects.

The full v0.2 tree will land immediately after this root commit.
