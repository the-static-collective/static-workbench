# COMPOSITION-ECOLOGY-001 — Toaster principles inside HOUSE

Status: **experimental, inert and stacked** on the exact rectified Workbench carrier PR #48. This is not a main-branch feature, Toaster runtime transplant, validated compositional adapter or release approval.

## Source ancestry and why this is not a second compositor

- Workbench Capability Return Ledger #41 → Return Shelf #43 → Capability Loom #46, reconciled by rectified carrier #48. The existing `preview_composition` validates exactly two source-owned artifacts and their exact Workbench-local return digests.
- Haunted Toaster's current README documents deterministic six-up, MUTATE / CROSS / STOMP / CONVERGE, KEEP / SCRAPE, L BRANCH, candidate lineage, and the human's separate continuation authority.
- Toaster design #226 and Workbench experimental #55 motivate bounded, attributable context routing. Workbench #55 is reference-only and is **not imported or represented as Toaster's renderer MixPlan**.
- Toaster archaeology #281 identifies the original ToastPack analysis manifest and disconnected ordinary Creative Context Table / Influence Diet socket as *frontiers*, not already-restored organs.
- Toaster HyperFood design #269 and experimental Video Phrase Grammar #282 / Relational Fit #287 are separately staged, not rendered or executed by HOUSE.

**Reuse the existing source ledger and Loom.** The new `composition_ecology.py` only derives another read-only projection from the same validated input. The existing Loom endpoint and reply remain unchanged.

## Present exact human path

HOUSE → Return Shelf → inspect existing self-reported returns → select exactly two distinct source-owned artifacts → enter one concrete question → **Preview three composition candidates** → inspect BRAID, CROSS, and MUTATE cards.

The existing **Preview selected composition** control remains available. Changing the selection or question clears stale previews. The new control shares the previous same-origin, local-session guard and refuses stale asynchronous responses. No artifact is selected by default. The candidate details are rendered as text, never interpreted as HTML.

The new POST `/api/house/ecology/preview` accepts precisely the same payload as `/api/house/loom/preview`:

```json
{
  "question": "Could these exact reported artifacts support a new instrument?",
  "selections": [
    {"return_id": "return-one", "local_digest": "64-character verified local return digest", "owner": "owner-one", "artifact_ref": "alpha"},
    {"return_id": "return-two", "local_digest": "64-character verified local return digest", "owner": "owner-two", "artifact_ref": "beta"}
  ]
}
```

This illustrative JSON is not a runnable request without two actual existing local return records and their exact digests.

### CapabilityPack v0: description, not discovery

Each exact Loom input yields one pack preserving its source-owner identity, original return identity and local digest, reported kind/capability/evidence and source nonclaims. Its input/output contracts are `unknown`, runtime adapter is `not_declared`, and source authenticity, compatibility, and authorization remain unevaluated. No file inspection, model, introspection or source code execution occurs.

### Three distinguishable bounded descendants

| Operator | Specimen hypothesis | Explicitly different plan |
| --- | --- | --- |
| BRAID | Could separately validated steps be sequenced? | Inspect parent 0; inspect parent 1; compare independently attributable outputs. |
| CROSS | Could a new explicit adapter combine distinct capabilities? | Discover both I/O contracts; design a bounded adapter; test a separate combined fixture. |
| MUTATE | Could one change be proposed without rewriting the base? | Inspect parent 0; let parent 1 influence a separately declared single change; compare to unchanged parent 0. |

These are **questions and phase labels**, not executable code or evidence that a source has the named capability. MUTATE's second parent is marked `influence_only`; no artifact bytes are copied and no properties are silently inherited.

Every candidate carries both exact source refs, its own digest, the parent Loom digest, its operator, local context diet, proposed phases, identity-only/influence-only inheritance, unavailable evidence, explicit nonclaims, and `execution=not_attempted`. The enclosing ecology also carries one local digest and `human_continuation=not_selected`.

A deterministic hash of a proposal is a **local repeatability fingerprint**, not cryptographic authentication of its source owner.

## Validation and effect boundary

Focused: `python -m pytest -q tests/test_composition_ecology.py tests/test_capability_loom.py tests/test_capability_returns.py tests/test_return_shelf.py`.

Complete: `python -m pytest -q` plus the existing CI syntax and application checks. Validate the **exact** combined head; a green ancestor is insufficient. On Linux, use two manually supplied fixture returns and check the new button, disabled state, three distinct cards, changing-question invalidation, and that the Return Ledger, repo checkout and operational event journal have no new composition effects.

No proposal is automatically KEPT or SCRAPED. UI inspection does not persist a descendant, train a preference, grant permission, run any adapter, create a worktree, call Toaster/Dogram/Wolfram, score candidates, publish content, or claim demonstrated compounded growth.

## Sliced continuation: seven planned organs, independent proof gates

1. **COMPOSITION-ECOLOGY-001 — present experimental proof.** Two exact parent refs → 3 inert, structurally distinct descendants; preserve existing Loom and authority boundaries. Pass pure/HTTP/UI tests and Linux browser witness before main promotion.
2. **CONTEXT-LANES-002 — explicit L BRANCH routing.** Reconcile Workbench #55 and #56 ancestry first. Add a versioned, finite source-lane bank with explicit use/ignore/influence-only to the candidate proposal. Verify lane identity, digest invalidation, refusal and no implicit promotion. Never call it Toaster's audio MixPlan.
3. **CAPABILITYPACK-003 — authenticated local capability descriptors.** Inspect a *named, allowlisted* source-owned interface/manifest at pinned Git identity; distinguish descriptive capability, demonstrated fixture compatibility and separately permitted effects. Integrate zero arbitrary manifest commands.
4. **PHRASE-GRAMMAR-004 — finite workflow phrasing.** Compile declared phases into a versioned, non-executing dependency graph with optional loops/re-entry and explicit completion/stop conditions. Check acyclicity of required dependencies and finite budgets. No automatic project execution.
5. **SELECTIVE-INHERITANCE-005 — one-delta descendant.** Human chooses a concrete eligible property to change, exact parent pins and immutable retained properties; reject unchanged/no-op transformations. Receipt both inherited and refused properties. No inferred preferences.
6. **ELSEWHERE-006 — attributable creative memory.** Query previously kept *exact* descendants by source, operator, and relationship receipts; surface an optional old influence only after user inspection. No automatic revival, global taste model, authority from similarity, or negative inference from rejection.
7. **RELATIONAL-FIT-007 / VERIFIED-RETURN-008 — budget then effects.** Fit a finite declared phase-weight pattern into finite time/context budgets, report rounding loss, preserve hard constraints. Only later use reviewed version-pinned project adapters and Staged Rocket under separate explicit execution authority, project-owned tests, interruption reconciliation, and demonstrated second-use receipts.

**Ordering law:** no future slice converts the present inert candidates into runnable plans without its own independently reviewed crossing. Source truth outranks this roadmap and the Toaster project's original code/receipts remain authoritative for Toaster.
