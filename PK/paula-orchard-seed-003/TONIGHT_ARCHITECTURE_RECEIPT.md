# Tonight scaffold — implementation receipt

- Based on the supplied `paula-orchard-seed-002-worldbody.zip` (containing the existing original Story Workbench and bounded local Orchard/Worldbody prototypes).
- Includes `ORCHARD_WORLD_BODY_SEEDS_003.md` as a separate **canon candidate**, not a merged or adopted law.
- Adds fillable first-use, private session and transfer templates, bounded staged builder prompts, a capability rack and an optional standalone local session board.
- Does not change existing server routes, model APIs, integration status, authorized creative decisions or package scripts.
- Does not create/sign a world identity, accept a seed on Paula's behalf, commit to GitHub, create a public registry, publish an artifact, or charge a customer.
- `tonight/SESSION_BOARD.html` has no remote scripts, no fetch calls and no automatic persistence. A deliberate manual JSON/Markdown export writes through the browser's download operation; file downloads can still be synced by the user's device or browser configuration. Do not input passwords/private keys. Raw story material remains local unless the user deliberately copies it into a chat/tool or another app.
- The original bundle's tests and existing `npm run check` remain authoritative for original functionality; the session board is not wired into the Story Desk and cannot claim to enforce its storage or sharing rules.
